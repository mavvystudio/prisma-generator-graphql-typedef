{
  "devDependencies": {
    "@commitlint/cli": "16.2.3",
    "@commitlint/config-conventional": "16.0.0",
    "husky": "7.0.4"
  },
  "scripts": {
    "prepare": "husky install",
    "build:generator":  "pnpm --filter prisma-generator-graphql-typedef build",
    "usage": "pnpm --filter usage start",
    "dev": "pnpm build:generator && pnpm usage"
  }
}
